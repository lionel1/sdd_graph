{
  "mcpServers": {
    "logseq": {
      "command": "npx",
      "args": ["-y", "@logseq/mcp-server"],
      "env": {
        "LOGSEQ_API_TOKEN": "${LOGSEQ_API_TOKEN}",
        "LOGSEQ_BASE_URL": "http://localhost:12315"
      }
    }
  }
}
